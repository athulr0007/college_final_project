<!DOCTYPE html>
<html lang="en">
<head>
    <title>Interview Summary</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>✅ Interview Summary</h1>
    <p>Thank you for participating in the mock interview. Below is a summary of your responses along with AI feedback.</p>

    <% if (interview && interview.responses && interview.responses.length > 0) { %>
        <table border="1">
            <tr>
                <th>#</th>
                <th>Question</th>
                <th>Your Answer</th>
                <th>AI Feedback</th>
            </tr>
            <% interview.responses.forEach((q, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td><%= q.question %></td>
                    <td><%= q.answer %></td>
                    <td><%= q.aiFeedback || "No feedback available" %></td>
                </tr>
            <% }); %>
        </table>
    <% } else { %>
        <p style="color: red;">⚠️ No responses found for this interview. Please try again later.</p>
    <% } %>

    <a href="/dashboard"><button>🏠 Back to Dashboard</button></a>
</body>
</html>
